<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:id="@+id/onboarding_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:orientation="vertical"
  android:padding="24dp"
  android:background="@android:color/transparent">

  <!-- Title -->
  <TextView
    android:id="@+id/tvTitle"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="Welcome to EV Charge"
    android:textColor="@color/m3_colorOnBackground"
    android:textSize="28sp"
    android:textStyle="bold"
    android:paddingBottom="8dp" />

  <!-- Content card -->
  <com.google.android.material.card.MaterialCardView
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="12dp"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="20dp"
    app:cardBackgroundColor="@color/brand_surface"
    app:strokeColor="@color/m3_colorPrimary"
    app:strokeWidth="0dp">

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:orientation="vertical"
      android:padding="20dp">

      <ImageView
        android:id="@+id/onbIllustration"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:contentDescription="@string/app_name"
        android:visibility="gone" />

      <TextView
        android:id="@+id/tvBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Find and book EV charging stations easily."
        android:textColor="#dcdcdc"
        android:textSize="16sp"
        android:lineSpacingExtra="4dp"
        android:paddingTop="12dp" />
    </LinearLayout>
  </com.google.android.material.card.MaterialCardView>

  <TextView
    android:id="@+id/tvStep"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="1 / 3"
    android:textColor="#dcdcdc"
    android:textSize="14sp"
    android:paddingBottom="12dp" />

  <LinearLayout
    android:id="@+id/buttonRow"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <com.google.android.material.button.MaterialButton
      android:id="@+id/btnBack"
      style="@style/Widget.Material3.Button"
      android:layout_width="0dp"
      android:layout_height="48dp"
      android:layout_weight="1"
      android:text="Back"
      android:textAllCaps="false"
      android:visibility="gone"
      app:cornerRadius="24dp"
      app:backgroundTint="@color/m3_colorPrimaryContainer"
      android:textColor="@color/m3_colorOnPrimary" />

    <com.google.android.material.button.MaterialButton
      android:id="@+id/btnSkip"
      style="@style/Widget.Material3.Button.OutlinedButton"
      android:layout_width="0dp"
      android:layout_height="48dp"
      android:layout_weight="1"
      android:layout_marginStart="8dp"
      android:layout_marginEnd="8dp"
      android:text="Skip"
      android:textAllCaps="false"
      app:cornerRadius="24dp"
      app:strokeColor="@color/m3_colorOutline"
      app:strokeWidth="1dp"
      app:backgroundTint="@color/brand_surface"
      android:textColor="@color/m3_colorOnSurface" />

    <com.google.android.material.button.MaterialButton
      android:id="@+id/btnNext"
      style="@style/Widget.Material3.Button"
      android:layout_width="0dp"
      android:layout_height="48dp"
      android:layout_weight="1"
      android:text="Next"
      android:textAllCaps="false"
      app:cornerRadius="24dp"
      app:backgroundTint="@color/m3_colorPrimary"
      android:textColor="@color/brand_on_primary" />
  </LinearLayout>
</LinearLayout>
